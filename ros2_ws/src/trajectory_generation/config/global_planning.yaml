trajectory_generation:
  ros__parameters:
    topics.odom: /localization/odometry
    topics.point_cloud: /filted_topic_3d
    topics.clicked_goal: /clicked_point
    topics.goal: /goal
    topics.waypoints: /waypoint_generator/waypoints
    topics.replan: /replan_flag
    topics.global_trajectory: /global_trajectory
    topics.target_result: /target_result
    frames.map: map
    maps.directory: map
    maps.occupancy: occfinal.png
    maps.bev: bevfinal.png
    maps.topology: occtopo.png
    maps.expected_width: 400
    maps.expected_height: 400
    planner.map_resolution: 0.05
    planner.map_x_size: 20.0
    planner.map_y_size: 20.0
    planner.map_lower_x: -13.394
    planner.map_lower_y: -12.079
    planner.robot_radius: 0.35
    planner.robot_radius_dash: 0.35
    planner.reference_desire_speed: 2.0
    planner.test_random_seed: -1
    planner.motion_mode: 1
    planner.enable_dynamic_cloud: true
    planner.replan_cooldown_seconds: 1.0
    planner.deviation_replan_distance: 1.0
